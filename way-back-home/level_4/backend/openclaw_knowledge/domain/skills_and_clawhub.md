# Skills and `clawhub`

- Skills extend OpenClaw with tools and workflows; installs are typically `clawhub install <slug>`.
- Only recommend slugs that appear in the **registry hints** returned by tools — do not invent package names.
- After installing skills, users often need to restart or reload the OpenClaw runtime per product docs.
