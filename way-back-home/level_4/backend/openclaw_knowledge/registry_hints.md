# Example registry lines (prototype — replace with your real `registry.md`)

Use these **only** as examples in generated guides; align with your organization’s real skill table in production.

| slug | Example install |
|------|-----------------|
| `agent-trust-hub` | `clawhub install agent-trust-hub` |
| `permission-gatekeeper` | `clawhub install permission-gatekeeper` |
| `snyk-scanner` | `clawhub install snyk-scanner` |

If the interview did not justify a skill, omit it rather than guessing.
